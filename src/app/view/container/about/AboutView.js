'use strict'

import RISView from '../../RISView'
import AboutTemplate from './AboutTemplate.hbs'

const AboutView = RISView.extend({
  className: '',
  template: AboutTemplate,
})

export default AboutView
